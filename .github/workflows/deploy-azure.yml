name: Deploy Backend to Azure App Service (DISABLED)

# This workflow is completely disabled to avoid npm ci errors
# The backend is a Python project, not a Node.js project

on:
  workflow_dispatch:
    # This workflow is disabled to avoid conflicts

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: This workflow is disabled
      run: |
        echo "This workflow is disabled to avoid npm ci errors"
        echo "The backend is a Python project, not a Node.js project"
        exit 0
