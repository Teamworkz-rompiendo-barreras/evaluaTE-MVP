name: Frontend Build and Deploy (DISABLED)

# DESHABILITADO - Usar frontend-deploy-optimized.yml en su lugar
on:
  workflow_dispatch:  # Solo permite ejecución manual, deshabilitando automático

# Este workflow está deshabilitado para evitar conflictos
# Usar frontend-deploy-optimized.yml en su lugar
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Deshabilitado
        run: |
          echo "❌ Este workflow está deshabilitado"
          echo "✅ Usar frontend-deploy-optimized.yml en su lugar"
          exit 1 