{
  "name": "evaluador-backend",
  "version": "1.0.0",
  "description": "Backend de EvaluaTE MVP",
  "main": "startup.py",
  "scripts": {
    "start": "python startup.py",
    "dev": "python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "echo 'No build step required for Python'",
    "test": "echo 'No tests configured'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["python", "fastapi", "azure"],
  "author": "Teamworkz",
  "license": "MIT"
} 