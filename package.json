{
    "name": "evaluate-root",
    "private": true,
    "scripts": {
        "build": "cd nuevo-frontend && npm install && npm run build",
        "install": "cd nuevo-frontend && npm install"
    }
}