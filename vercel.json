{
    "buildCommand": "cd nuevo-frontend && npm install --no-audit && export NODE_OPTIONS=--max-old-space-size=4096 && npx vite build",
    "outputDirectory": "nuevo-frontend/dist",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        },
        {
            "source": "/((?!api/).*)",
            "destination": "/index.html"
        }
    ]
}